import { BodyThemeDirective } from './body-theme.directive';

describe('BodyThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyThemeDirective();
    expect(directive).toBeTruthy();
  });
});
