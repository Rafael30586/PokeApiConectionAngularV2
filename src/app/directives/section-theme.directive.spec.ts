import { SectionThemeDirective } from './section-theme.directive';

describe('SectionThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionThemeDirective();
    expect(directive).toBeTruthy();
  });
});
