import { RotationButtonDirective } from './rotation-button.directive';

describe('RotationButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RotationButtonDirective();
    expect(directive).toBeTruthy();
  });
});
