import { TitleSectionDirective } from './title-section.directive';

describe('TitleSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleSectionDirective();
    expect(directive).toBeTruthy();
  });
});
