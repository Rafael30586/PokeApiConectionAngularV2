import { SubmitButtonThemeDirective } from './submit-button-theme.directive';

describe('SubmitButtonThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitButtonThemeDirective();
    expect(directive).toBeTruthy();
  });
});
