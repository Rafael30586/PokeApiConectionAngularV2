import { ImageOrientationDirective } from './image-orientation.directive';

describe('ImageOrientationDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageOrientationDirective();
    expect(directive).toBeTruthy();
  });
});
