import { HeaderThemeDirective } from './header-theme.directive';

describe('HeaderThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderThemeDirective();
    expect(directive).toBeTruthy();
  });
});
