import { RemoveMiddleDashPipe } from './remove-middle-dash.pipe';

describe('RemoveMiddleDashPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveMiddleDashPipe();
    expect(pipe).toBeTruthy();
  });
});
