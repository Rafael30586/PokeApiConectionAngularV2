import { InputThemeDirective } from './input-theme.directive';

describe('InputThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputThemeDirective();
    expect(directive).toBeTruthy();
  });
});
