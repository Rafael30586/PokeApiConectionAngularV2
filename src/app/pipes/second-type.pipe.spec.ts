import { SecondTypePipe } from './second-type.pipe';

describe('SecondTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondTypePipe();
    expect(pipe).toBeTruthy();
  });
});
