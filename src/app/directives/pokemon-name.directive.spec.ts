import { PokemonNameDirective } from './pokemon-name.directive';

describe('PokemonNameDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonNameDirective();
    expect(directive).toBeTruthy();
  });
});
