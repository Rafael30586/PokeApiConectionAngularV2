import { IconThemeButtonDirective } from './icon-theme-button.directive';

describe('IconThemeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new IconThemeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
