import { ThemeButtonThemeDirective } from './theme-button-theme.directive';

describe('ThemeButtonThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeButtonThemeDirective();
    expect(directive).toBeTruthy();
  });
});
